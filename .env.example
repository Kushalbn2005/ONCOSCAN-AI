# Comma-separated browser origins allowed to call the API
# FRONTEND_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173

# Heatmap mode: light (default on Render), full (Grad-CAM), or off
# HEATMAP_MODE=light
